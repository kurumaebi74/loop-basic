# loop-basic — 自律開発エージェント運用マニュアル

このリポジトリは「調査 → 設計 → 製造 → テスト」を自律的に回す開発エージェントのための基本ファイル群を管理する。**設計の直後**と**テストの直後**の2箇所に人間による確認ゲートを設ける。それ以外のフェーズ遷移は自律的に進めてよい。加えて、設計完了後と実装完了後にはそれぞれ**別のサブエージェントによる自動レビュー**(最大2ラウンド)を挟む。レビューは人間確認ゲートではなく完全に自律的なチェックだが、解消しきれなかった指摘は直後の人間確認ゲートに必ず引き継ぐ。さらに、実装フェーズでは**メインエージェント(オーケストレーター)自身はコードを書かない**。必ず `TaskCreate` で設計をタスクに分割し、依存関係のないタスクは `implementer` サブエージェントへ並列dispatchし、`TaskList`/`TaskUpdate` で進捗を確認する役割に徹する。

## ワークフロー全体像

```
/investigate <topic>   調査          … 自律実行、成果物を docs/investigations/ に保存
        ↓
/design                 設計          … 自律実行、成果物を docs/designs/ に保存
        ↓
   [設計レビュー]         ← design-reviewer による自動レビュー、最大2ラウンド(人間は介在しない)
        ↓
   [人間確認ゲート]      ← ここで必ず停止し、人間の承認を待つ(未解決レビュー指摘があれば提示)
        ↓
/implement               製造(実装)   … トピック専用の git worktree 内で、TaskCreateによるタスク分割 → implementerへ並列dispatch(メインエージェントは実装しない)
        ↓
   [コードレビュー]       ← code-reviewer による自動レビュー、最大2ラウンド(人間は介在しない)
        ↓
/test                    テスト        … 同じワークツリー内で自律実行、結果を docs/tests/ に保存
        ↓
   [人間確認ゲート]      ← ここで必ず停止し、人間の承認を待つ
        ↓
   承認時のみベースブランチへマージ・ワークツリー削除
        ↓
     完了 / 次サイクルへ
```

`/cycle <topic>` はこの一連の流れを最初から最後まで（2つのゲートを含めて）通しで実行するオーケストレーションコマンド。個別フェーズだけを実行したい場合は `/investigate`・`/design`・`/implement`・`/test` を単体で使う。

## 厳守ルール(ゲート)

1. **設計完了後、人間の明示的な承認(Approve)を得るまで `/implement` を自動実行してはならない。** 承認・差し戻し・修正依頼のいずれかが人間から得られるまで停止する。
2. **テスト完了後、人間の明示的な確認を得るまでサイクルを完了(クローズ)してはならない。** 不合格・要修正の場合は `/design` または `/implement` に戻る。
3. ゲートでの確認には `AskUserQuestion` ツールを使い、選択肢(承認/差し戻し/修正依頼など)を明示すること。曖昧な「進めてよいですか?」で終わらせない。
4. 各フェーズの成果物は必ずファイルとして残す(口頭やチャットのみで済ませない)。次フェーズはそのファイルを読んで開始する。
5. 調査・製造・テストのフェーズ内で致命的な不確実性(要件不明、認証情報が必要、破壊的操作が必要 等)にぶつかった場合は、そのフェーズ内であっても停止して人間に確認してよい。
6. **`/implement` を実行しているメインエージェントは、自分でファイルを編集・作成して実装してはならない。** 必ず `TaskCreate` でタスクに分割し、`implementer` サブエージェントに委譲する。タスクが1つしかない場合も例外なく委譲する(詳細は「実装フェーズのタスク分割・並列化」節)。

## 設計・実装のレビューループ(自動、最大2ラウンド)

`/design` の設計ドキュメント作成後(人間確認ゲート1の前)と、`/implement` の実装完了後(`/test` の前)には、それぞれ作成者とは別のサブエージェントによるレビューを挟む。**このレビューは人間を介さない自律的なチェックであり、新たな人間確認ゲートを追加するものではない。** 人間確認ゲートは引き続き2箇所(設計後・テスト後)のみ。

### 仕組み

1. 設計後: `design-reviewer` が設計ドキュメント(と対応する調査ドキュメント)をレビューし、`PASS` か `MAJOR` を判定する。
2. 実装後: `code-reviewer` が設計ドキュメントとワークツリー内の実装差分をレビューし、同様に判定する。
3. `MAJOR` の場合、作成者(designer/implementer、または呼び出し元)が指摘に基づいて修正し、同じレビューアーに再レビューを依頼する(ラウンド2)。
4. ラウンド2の結果に関わらず、それ以上は自動でループしない(最大2ラウンド)。ラウンド2で `MAJOR` が残った場合は、その指摘を隠さず**直後の人間確認ゲートの提示内容に明記**し、最終判断を人間に委ねる。
5. 各ラウンドの判定・主な指摘は、設計ドキュメントの「設計レビュー結果」「コードレビュー結果」欄、テストレポートの「コードレビューからの引き継ぎ」欄に記録する。

### レビューアーの独立性

`design-reviewer`・`code-reviewer` は自分では設計・実装を行わない、レビュー専任のサブエージェントである。指摘のみを行い、修正は元の担当(designer/implementer)に委ねる。「書いた本人が自分のミスに気づけない」状態を避けるのが目的なので、レビューを著者自身に兼任させない。

## ディレクトリ構成

- `docs/investigations/` … 調査結果(1トピック1ファイル、`YYYY-MM-DD-<topic-slug>.md`)
- `docs/designs/` … 設計ドキュメント(調査と同じslugで対応付ける)
- `docs/tests/` … テスト結果レポート
- `docs/templates/` … 各成果物のテンプレート。新規作成時は必ずこれをコピーして使う
- `docs/memory/` … サブエージェント・フェーズ横断の**共有メモリ**(詳細は次節)
- `docs/investigations/_parts/<topic-slug>/` … 調査フェーズを並列化(ファンアウト)した際の、観点ごとの作業ファイル置き場(詳細は次々節)
- `.claude/worktrees/<topic-slug>/` … 実装・テストフェーズ用に分離された git worktree(詳細は後述)。`.gitignore` 対象で、通常はマージ後に削除される
- `.claude/commands/` … `/investigate` `/design` `/implement` `/test` `/cycle` の定義
- `.claude/agents/` … 各フェーズを担当するサブエージェント定義(investigator / designer / implementer / tester / design-reviewer / code-reviewer)
- `.claude/settings.json` … プロジェクト共有設定。`worktree.baseRef: "head"` を設定済み(このリポジトリにはリモートがないため、ワークツリーはローカルHEADから分岐する)

## 共有メモリ(エージェント間の記憶共有)

サブエージェントはそれぞれ独立した会話コンテキストで起動され、他のサブエージェントの作業内容やこれまでの会話履歴を直接は覚えていない。フェーズをまたいで使い回すべき知識(決定事項・規約・落とし穴・用語の定義など)を毎回ゼロから調べ直したり、矛盾した判断をしたりしないよう、`docs/memory/` をエージェント間の**共有された長期記憶**として運用する。

### 構成

- `docs/memory/MEMORY.md` … 索引。1エントリ1行、カテゴリ別(decision / convention / pitfall / glossary / open-question)に `entries/<name>.md` へのリンクを列挙する。内容そのものはここに書かない。
- `docs/memory/entries/<name>.md` … 個々のメモリエントリ本体。`docs/memory/templates/entry-template.md` の形式(frontmatter + 結論 + 背景 + 適用範囲)に従う。
- `docs/memory/templates/entry-template.md` … エントリ作成時に必ずコピーして使うテンプレート。

### カテゴリの意味

- **decision**: 特定のtopicに閉じない、プロジェクト全体に及ぶ技術的決定(例: 採用ライブラリ、全体アーキテクチャ方針)
- **convention**: 命名規則・実装スタイル・テストの書き方など、以降すべての実装が従うべき規約
- **pitfall**: 実装・テストの過程で判明した既知の落とし穴、同じ失敗を他のtopic/フェーズで繰り返さないための教訓
- **glossary**: プロジェクト固有の用語・略語の定義(人間とエージェント、エージェント間で解釈がズレないようにする)
- **open-question**: 単一topicの調査・設計だけでは決められない、複数topicに影響する未解決の疑問(いずれ人間判断が必要)

### 読み書きのルール

1. **すべてのサブエージェント・コマンドは、作業開始前に必ず `docs/memory/MEMORY.md` を読む。** 関連しそうなエントリがあれば `entries/` 配下の該当ファイルも読み、既存の決定・規約・落とし穴と矛盾しないように作業する。
2. フェーズをまたいで再利用可能な知識を得た(あるいは既存の記憶と矛盾する事実を発見した)場合、作業完了時に `docs/memory/entries/` にエントリを追加・更新し、`MEMORY.md` の索引にも追記する。
3. **既存エントリと矛盾する結論に至った場合は、古いエントリを削除せず `status: superseded` にして残し、新しいエントリから経緯が追えるようにする。** 記憶を上書きで消さない。
4. 同じ内容のエントリを重複して作らない。追加前に `MEMORY.md` を確認し、既存エントリがあれば新規作成ではなく更新(`updated` 日付の更新含む)を行う。
5. 各成果物ドキュメント(調査・設計・テストレポート)に書くべき内容(そのtopic固有の詳細)を、共有メモリに重複して書かない。共有メモリに置くのは**そのtopicを超えて再利用される知識だけ**。
6. 承認判断・ゲートの通過記録など、人間の意思決定そのものは各フェーズの成果物ドキュメント側(設計ドキュメント/テストレポートの「人間確認ゲート記録」欄)に残し、共有メモリには書かない。共有メモリはあくまで再利用可能な知識のためのもの。

## 調査フェーズの並列化(ファンアウト)

1つのトピックの中に、互いの結果を待たずに独立して調べられる観点が複数ある場合(例:「ライブラリ選定」「既存アーキテクチャとの整合性」「パフォーマンス制約」)、`/investigate` は `investigator` サブエージェントを**同一メッセージ内で複数回呼び出す並列dispatch**によって観点ごとに分担調査させてよい。詳細な手順は `.claude/commands/investigate.md` に定義されている。要点は以下。

- 分解は観点が真に独立している場合のみ行う。観点同士が依存する場合や観点が1つしかない場合は分解しない。分解しても2〜4観点程度に留める。
- 各投入先には出力パス(`docs/investigations/_parts/<topic-slug>/<sub-slug>.md`)を明示し、investigator自身にパスを決めさせない。
- **並列実行中のサブエージェントは `docs/memory/` に直接書き込まない。** 複数エージェントが同時に `MEMORY.md` を編集すると内容が競合・上書きされるため、各エージェントは再利用可能な知見を「メモリ候補」として最終報告に含めるに留め、実際の書き込み(重複排除を含む)は全員の完了後にorchestrator(呼び出し元のメインの会話)が1回でまとめて行う。
- 全観点の結果が揃ったら、矛盾がないか確認したうえで単一の `docs/investigations/YYYY-MM-DD-<topic-slug>.md` に統合する。観点間で結論が食い違う場合は黙って一方を採用せず「未解決の疑問」として残す。
- この並列化パターンは調査フェーズ専用に定義しているが、設計・実装・テストの各フェーズで同様に独立したサブタスクへの分解が可能だと判断した場合、同じ原則(出力パスの明示、共有メモリへの書き込みはorchestratorに集約)を踏襲して拡張してよい。

## 実装フェーズのワークツリー分離(複数セッション対応)

このプロジェクトは、複数のセッション(異なるターミナル/チャットから起動された複数のClaude Codeインスタンス)が同時に同じリポジトリを修正することを想定している。共有の作業ディレクトリを複数セッションが同時に書き換えると、チェックアウト中ブランチの奪い合いやコミットの混線が起きるため、**コード変更を伴う `/implement` フェーズは必ずトピック専用の git worktree 内で行う。**

### 仕組み

1. `/implement` は実装を始める前に、ベースブランチの未コミット変更(直前の調査・設計ドキュメントなど)を先にコミットしたうえで、`EnterWorktree`(`name: <topic-slug>`)でトピック専用のワークツリー(`.claude/worktrees/<topic-slug>`、専用ブランチ)に入る。分岐元は `.claude/settings.json` の `worktree.baseRef: "head"` によりローカルHEAD(このリポジトリにはリモートがないため)。
2. `/implement`・`/test` はこのワークツリー内で実装・コミット・テストを行う。実装フェーズでは複数の `implementer` が並列で動くことがあるため、コミットは原則としてメインエージェントがタスク単位でまとめて行う(詳細は「実装フェーズのタスク分割・並列化」節)。ベースブランチとは分離されているため、こまめなコミット自体は安全。
3. `/test` のゲート2で人間が「承認してクローズ」を選んだ場合にのみ、`ExitWorktree(action: "keep")` でベースブランチに戻り、`git merge <topic-branch>` でベースブランチへ統合し、成功したらワークツリーとブランチを削除する。マージがコンフリクトした場合は自動解決せず人間に報告する。
4. ゲート2で「修正依頼」「設計からやり直し」が選ばれた場合は、ワークツリーを削除せずそのまま残し、同じブランチ内で作業を継続する。

### 複数セッションでの並行作業

セッションAがtopic Xを、セッションBがtopic Yを同時に `/cycle` または `/implement` している場合、それぞれ別のワークツリー(`.claude/worktrees/topic-x`、`.claude/worktrees/topic-y`)・別のブランチで作業するため、実装フェーズでのファイル競合は起きない。ただし以下は既知の制限であり、このリポジトリでは未対応:

- `/investigate`・`/design` はワークツリー化しておらず、ベースブランチの共有ディレクトリで直接ファイルを書く。複数セッションが同時に投資調査・設計ドキュメントを書く場合、特に `docs/memory/MEMORY.md`(索引ファイル)への同時書き込みは競合しうる。
- 複数トピックのブランチが同時にベースブランチへマージされる場合、`docs/memory/MEMORY.md` など共有ファイルでマージコンフリクトが起きる可能性がある。この場合もゲート2の手順どおり自動解決せず人間に報告する。

## 実装フェーズのタスク分割・並列化

`/implement` を実行するメインエージェント(オーケストレーター)は、**自分でコードを実装しない。** 設計ドキュメントの実装ステップを `TaskCreate` でタスクに分割し、依存関係のないタスクは `implementer` サブエージェントへ並列dispatchし、`TaskList`/`TaskUpdate` で進捗を確認・記録する役割に徹する。詳細な手順は `.claude/commands/implement.md` に定義されている。要点は以下。

- タスク分割は必須。実装ステップが1つしかない場合でも、必ず `TaskCreate` でタスク化してから `implementer` に委譲する。メインエージェントが直接ファイルを編集することはない。
- タスク間の依存関係は `TaskUpdate` の `addBlockedBy`/`addBlocks` で明示する。**同じファイルを変更する複数のタスクは並列化せず、順序づける。** 依存がなく変更対象ファイルも重ならないタスクだけを並列化の対象にする。
- 依存の解決済みタスク群(ウェーブ)は、`implementer` を**同一メッセージ内で複数回呼び出して並列実行**する。各`implementer`は着手時に `TaskUpdate` で `owner`・`status: in_progress` を設定し、完了時に `status: completed` にする(未完了・エラー残りは `completed` にしない)。
- **並列dispatch中の `implementer` は `git commit` を自分では行わない。** 同一ワークツリー内での競合を避けるため、ファイル変更のみ行い、メインエージェントがタスク完了報告を受けるたびにコミットする。コミットは実装ではなく記録作業なので、これはメインエージェント自身が行ってよい。
- 並列dispatch中は `implementer` も共有メモリへ直接書き込まない(ファンアウトと同じ理由・同じ原則)。メモリ候補として報告し、メインエージェントがウェーブ完了ごとに集約して反映する。
- コードレビュー(`code-reviewer`)は全タスク完了・コミット後に1回行う。`MAJOR`判定の修正も新しいタスクとして`TaskCreate`し`implementer`に委譲する(メインエージェントが直接修正しない)。

## 成果物の命名規則

`YYYY-MM-DD-<topic-slug>.md` で統一する。同一トピックの調査・設計・テストは同じslugを使い、相互にリンクを張ること(例: 設計書の冒頭に対応する調査ドキュメントへのリンクを置く)。

## 開発・検証コマンド

このリポジトリ自体は現時点でアプリケーションコードを持たない(エージェント運用ファイルのみ)。実装対象プロジェクトが追加されたら、ここにビルド・テストコマンド(例: `npm test`, `pytest` 等)を追記すること。`/test` コマンドはこのセクションを参照して実行コマンドを決定する。

### 手動確認の使い分け(自動テストが無い/不十分な場合)

- **API・バックエンド**: `curl` で実際にエンドポイントを叩いて確認する。
- **フロントエンド(画面・UI)**: Playwright CLI(`npx playwright test` 等)で確認する。
- どちらにも当てはまらない対象(バッチ処理・スクリプト等)は、妥当な方法(直接実行など)で確認する。

`/test` および `tester` はこの使い分けに従うこと。

## サブエージェントの使い分け

- **investigator**: 読み取り専用に近い調査(Web検索・コード探索)。実装はしない。
- **designer**: 調査結果を踏まえて設計書を書く。コードは書かない。
- **design-reviewer**: designerとは別の視点で設計書をレビューする。自分では書き直さない。
- **implementer**: `TaskList`で割り当てられた単一タスクの範囲だけを実装する。設計にない範囲を勝手に広げない。並列dispatch中は自分でコミットも共有メモリへの書き込みも行わない。
- **code-reviewer**: implementerとは別の視点で実装をレビューする。自分では修正しない。
- **tester**: 実装を検証し、レポートを書く。テストを通すためにテスト自体を歪めない(テストコードの改変は原則implementerの担当に戻す)。

各フェーズはメインの会話コンテキストを汚さないよう、可能な限り対応するサブエージェント(Agent tool)に委譲する。**特に実装フェーズは、メインエージェントが自分で実装することを禁止し、`TaskCreate`/`TaskUpdate`/`TaskList`によるタスク分割・並列dispatch・進捗確認を必須とする。**
